<!-- Heading Content -->
<% if (title) { %>
    <h1><%= title %></h1>
  <% } else {
    res.redirect('/')
    } %>
  
  <!-- Message Block -->
  <%# messages() %>
  
  <!-- Vehicle Display -->
  <%- grid %>